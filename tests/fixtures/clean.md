---
provenance: enforced
---
# Clean
## Observed — things
- Both repositories were empty at capture time. [src:REPO-EMPTY-2026-08-27]
> Framing lines are skipped by design.
## Design
This section is not an Observed section, so it needs no tag.

## Design
Naming the phrase `as we discussed` in inline code is allowed.
An example tag like `[src:NOT-REAL]` in code need not resolve.
