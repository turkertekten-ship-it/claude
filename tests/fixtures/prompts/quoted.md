You are a linter author documenting rules.

Write a rule description for the phrase `as we discussed`, which the guard
rejects, and for `TODO` markers.

Output: one sentence per rule, as a markdown list, at most 40 words.

Acceptance test: both phrases appear inside backticks and nowhere else.

If a phrase is not in the rule table, say so instead of inventing a rule.
