Parity conformance — executed, not asserted
========================================================================
[PASS] System prompt replaces the default
       the replaced system prompt took effect: asked for a designation, got 'LIGHTHOUSE-7'
[PASS] {{variable}} templating
       rendered 'Hello A, you are B.'; unfilled placeholder raises
[PASS] Prompt versions are diffable artifacts
       doctrine-adherence.yaml has 4 revision(s) in git history; a saved prompt in a console cannot be diffed or reverted
[PASS] Model selection
       requested claude-haiku-4-5, backend reported canonicalModel='claude-haiku-4-5'
[PASS] Effort control changes work done
       thinking tokens: low=213, high=195
[PASS] Thinking budget (undocumented flag)
       --thinking/--max-thinking-tokens accepted and the call succeeded
[PASS] Max output tokens
       a 64-token ceiling on a long task was enforced by refusal ("API Error: Claude's response exceeded the 64 output token maximum. To "), and an 8000-token ceiling let the same shape of task through. Enforced by erroring, not by truncating — which is why an earlier version of this check wrongly called it broken.
[PASS] Budget ceiling is enforced
       --max-budget-usd 0.0001 on a deliberately long task -> stop_reason='', error='error_max_budget_usd', spent=$0.000964
[----] Custom tool definitions
       the CLI can switch its own built-in tools on and off but cannot define one with your schema. Built on the anthropic-api backend and driven over real HTTP in the test suite, including surfacing the resulting tool_use block. Missing: a credential.
[----] Prompt caching
       cache_control on the system prompt — the largest cost lever for a suite that re-sends the same prefix on every case. Built and wire-tested; the response's cache_read_input_tokens is already parsed. Missing: a credential.
[----] Image and document input
       attachments lead the first user turn before the text, which is the documented order (['image', 'text']). Built and wire-tested. Missing: a credential.
[----] Direct Messages API access
       no credential source found: ANTHROPIC_API_KEY unset, the `ant` CLI is not installed, and neither ~/.config/anthropic nor ~/.claude/.credentials.json exists. The OAuth token file descriptor the environment names resolves to pipe:[847] — a one-shot stream already consumed by the CLI, not a credential store. This is why count_tokens, the Batch API and stop_sequences are unreachable: established by looking for each source, including that one, not inferred from an absent variable. All three are implemented on the anthropic-api backend and would run against a credential.
[----] temperature / top_p / top_k
       no CLI flag (--temperature rejected, --top-p rejected, --top-k rejected) and on models after Opus 4.6 the API rejects them with a 400 — removed from the platform, not missing from this tool. On models that predate that, the anthropic-api backend sends them and refuses to send them to a model that would 400; verified offline, uncredentialed here.
[----] stop_sequences
       no CLI flag. BUILT on the anthropic-api backend and exercised over real HTTP against a conforming server, which received ['STOP'] in the request body. What is missing is a credential for Anthropic's endpoint, not code and not a tested transport.
[PASS] Structured output against a schema
       structured_output={"city": "Paris", "population": 2161000}; schema errors: none
[PASS] Tool availability is controllable
       --tools "" produced a single-turn response (num_turns=1), i.e. no tool loop
[PASS] Request is inspectable before sending
       `workbench plan` resolved every request without sending: 18 model call(s) would be made.
[PASS] Token counts and cost are reported
       in=248 out=55 cost=$0.00146 — provider-reported, not estimated from a price table
[----] count_tokens before sending
       /v1/messages/count_tokens is implemented and driven over real HTTP in the test suite: it returns a count and omits max_tokens, which that endpoint rejects. Missing: a credential, not code.
[----] Batch API 50% discount
       /v1/messages/batches is implemented and driven over real HTTP in the test suite, keyed by custom_id since results return out of order. Missing: a credential, not code.
[PASS] Eval grid over test cases
       3 variants x 6 cases graded offline on the echo backend
[PASS] Blind comparison with position swap
       a judge that always picks the first candidate was correctly downgraded to 'TIE' (agreed=False) — the swap caught it
[PASS] Identical-pair blinding control
       a tying judge passes the control and a judge that picks a winner between two identical candidates fails it
[PASS] Significance testing
       4-2 -> p=0.688 (not significant), 10-0 -> p=0.0020 (significant), 4/4 -> CI [0.510, 1.000]; ~47 pairs needed for a 70/30 effect
[PASS] Grade by shell command (outcome-based)
       the repository's own fabrication guard passed an honest answer and failed an invented one, by exit code
[PASS] Export to claude plugin eval
       emitted 6 case(s) and 12 grader file(s) in the format `claude plugin eval` reads
[PASS] claude plugin eval is available
       present and runnable; its ablation is with-plugin vs without-plugin, not N prompt variants
[PASS] skill-creator is available
       first-party blind comparator at /mnt/skills/examples/skill-creator: withholds which skill produced which output, and its protocol describes a single fixed-order judgement — use it for skills; use this workbench when the comparison needs the swap, more than two variants, or a p-value
========================================================================
20 passed, 0 failed, 8 unreachable — $0.0232, 72s

Unreachable is not a softer failure. Those capabilities either need
an API key this container does not have, or were removed from the
platform and cannot be exercised anywhere.
